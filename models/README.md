# Models

Place your exported ONNX detector models in this folder, for example:

- `models/yolov8n.onnx`
- `models/yolov8s.onnx`

The edge configs reference `models/yolov8n.onnx` as the default detector path for the external ONNX hook.
